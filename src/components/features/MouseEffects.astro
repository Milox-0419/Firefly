---
// 鼠标交互特效
// - 拖尾圈光标：紫色内圆点即时跟手 + 多个外圈依次追逐前一环，
//   透明度/尺寸逐级衰减，形成"从光标拖出一串圈"的效果（自定义实现，替代系统光标）
// - Ripple：点击半透明波纹（mouse-animations 库）
// 仅在精确指针设备（桌面端）启用，触屏/手机自动跳过；尊重"减弱动态效果"偏好
// 脚本通过 public/scripts/cursor-effect.js 加载，支持 Swup 页面切换后重新初始化
---

<!-- 波纹样式静态副本：与库运行时注入 head 的规则完全一致。
     库注入的 <style> 会被 Swup 页面切换时的 head 重建丢弃，
     而本静态样式由 Astro 编译进持久样式表，切换后依然生效 -->
<style is:global>
	@keyframes __ma-ripple-expand {
		from {
			transform: scale(0);
			opacity: 1;
		}
		to {
			transform: scale(1);
			opacity: 0;
		}
	}
	.__ma-ripple-el {
		position: fixed;
		border-radius: 50%;
		pointer-events: none;
		animation: __ma-ripple-expand var(--_dur) ease-out forwards;
	}
</style>

<script>
	// 这个组件不再包含初始化逻辑，逻辑已移至 public/scripts/cursor-effect.js
	// 在这里只需确保脚本被正确加载（通过 Layout.astro 中的 script 标签）
	// 保留此文件是为了兼容现有的组件引用结构
</script>
